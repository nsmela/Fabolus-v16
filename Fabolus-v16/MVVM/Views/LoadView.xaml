<UserControl x:Class="Fabolus_v16.MVVM.Views.LoadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:Fabolus_v16.MVVM.ViewModels"
             xmlns:local="clr-namespace:Fabolus_v16.MVVM.Views"
             d:DataContext="{d:DesignInstance Type=viewModels:LoadFileViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200" Background="{DynamicResource MahApps.Brushes.Button.Flat.Background}">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
        <Button Content="load" 
                Height="30"
                HorizontalAlignment="Stretch"
                Style="{StaticResource MahApps.Styles.Button}"
                FontSize="14" 
                FontWeight="Bold"
                FontFamily="\Fonts\#Poppins"
                Command="{Binding LoadFileCommand}" />
        <Button Content="import" 
                Height="30"
                HorizontalAlignment="Stretch"
                Style="{StaticResource MahApps.Styles.Button}"
                FontSize="14" 
                FontWeight="Bold"
                FontFamily="\Fonts\#Poppins"
                Command="" />
        
    </StackPanel>
</UserControl>
